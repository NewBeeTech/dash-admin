
import React, { PropTypes } from 'react';

const Shouye = (props) => {
  return (
    <div>
      登录首页
    </div>
  );
};
Shouye.propTypes = {
  dispatch: PropTypes.func.isRequired,
};

export default Shouye;
